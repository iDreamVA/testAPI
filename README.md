# How I use it
Initially, I am using **Ngrok** to create a temporary URL to connect with the LINE webhook. Once I receive the **image ID from the LINE webhook**, I will call the **API** to retrieve the images sent by users and automatically store these images in a data repository. This will facilitate systematic image management from LINE and simplify future usage.
